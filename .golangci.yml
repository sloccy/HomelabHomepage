version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - copyloopvar
    - gocritic
    - gosec
    - misspell
    - noctx
    - unparam

formatters:
  enable:
    - gofmt
    - goimports
